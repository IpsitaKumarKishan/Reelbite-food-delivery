# Project Rules for Reelbite

## Git Workflow
- Create a clean local Git commit whenever a logical unit of good code changes, feature addition, or refactoring is completed (commit by feature/change completion, not by time interval).
- **Do NOT push to GitHub automatically**. Only create local Git commits. Remote push (`git push`) must ONLY occur when the user explicitly requests it.
